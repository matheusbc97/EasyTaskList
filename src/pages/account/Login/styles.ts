import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  buttonWrapper: {
    width: '100%',
    alignItems: 'center',
    paddingVertical: 15,
  },
  formWrapper: {
    padding: 20,
    alignItems: 'center',
    backgroundColor: '#fafafa',
    marginTop: -50,
  },
});
